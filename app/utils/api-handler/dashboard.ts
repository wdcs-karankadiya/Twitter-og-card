// Mock API handler for user stats
export async function getuserStats(userId: number) {
  // This would normally make an API call
  // For now, we'll return null since we're using static data in the route
  return null
}
